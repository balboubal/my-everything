[build]
builder = "nixpacks"
 
[build.nixpacks]
installCmd = "npm install && cd client && npm install && cd ../server && npm install"
buildCmd = "cd client && npx vite build"
startCmd = "cd server && node index.js"
 
[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10