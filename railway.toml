[build]
builder = "nixpacks"

[build.nixpacks]
installCmd = "npm install && cd client && npm install && npm run build && cd ../server && npm install"
startCmd = "cd server && node index.js"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
